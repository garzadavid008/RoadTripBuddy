package com.example.roadtripbuddy.databaseTests

class FirestoreTest {

    // I need to check if UserViewModel.kt or TripViewModel.kt uses FirestoreTest.kt
    // If firestore is used prioritize the tests for this.
    // if neither uses it just delete it.

}

/*This Test(if applicable):
* Read/Write operations
* Handling Errors (Offline or denied access
* Syncing and real-time updates
* */