package com.example.roadtripbuddy.mapfunctionalityTests


class MapFunctionalityTest {

    // creates mocks for GooglePlaces.kt, LocationService.kt, PlanATripViewModel,kt
    /* @Test
     fun map_rendersCorrectly() {
         composeTestRule.setContent {
             MapComponent()
         }
         composeTestRule.onNodeWithTag("map_view").assertExists()
     }*/

    // Ensure Mapcomponent.kt or planmap.kt has tests tags will work on it later,=.

}

/*This Tests(TomTom/GoogleMaps SDK):
* If the Map is rendered correctly
* Markers appear based on data
* User can interact with the map
* Route or location updates appear correctly
* */